<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        <title>{% block title %}rockNtro!{% endblock %}</title>
       
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
        {# CDN Bootstrap #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/site.css') }}">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css"
    </head>

    <body>
        <div class="container">
            <header class="blog-header py-3">
                <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4 pt-1">

               {% if app.user %}
                            <span>Bonjour {{ app.user.pseudo}}</span>
                {% else %}
                            <a href="{{ path('app_register') }}" class="link-secondary">Créer votre compte</a>
                {% endif %} 


                </div>
                    <div class="col-4 text-center">
                        <a class="blog-header-logo text-dark fst-italic" href="{{ path('home') }}"><img src="{{ asset('assets/logo_small.png') }}" alt=""></a>
                        <p class="lead my-3"><strong>Ici vous trouverez le meilleur Top 50 des intros</strong></p>
                </div>
                
                <div class="col-4 d-flex justify-content-end align-items-center">
                    {# <a class="link-dark" href="#" aria-label="Search"> #}
                    {# <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="mx-3" role="img" viewBox="0 0 24 24"><title>Search</title><circle cx="10.5" cy="10.5" r="7.5"/><path d="M21 21l-5.2-5.2"/></svg>
                    </a>
                     #}
                 
                     {% if app.user %}

                        {# {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('admin') }}" class="btn btn-sm btn-outline-secondary">BACKOFFICE</a>                      
                        {% endif %} #}

                        <a href="{{ path('app_logout') }}" class="btn btn-sm btn-outline-dark">Se deconnecter</a>
                    {% else %}
                        
                        <a href="{{ path('app_login') }}" class="btn btn-sm btn-outline-dark ml-2">Identifiez-vous</a>
                    {% endif %}
                </div>

            </header>

            <div class="nav-scroller py-1 mb-2">
                <nav class="nav d-flex justify-content-evenly">
                <a class="p-1 link-secondary" href="{{ path('home') }}"><i class="bi bi-house-door-fill"></i></a>
                <a class="p-1 link-secondary" href="{{ path('proposition')}}">Proprosition</a>
                <a class="p-2 link-secondary" href="{{ path('app_login') }}">Connexion</a>

                {# path() : fonction TWIG permettant de générer une route, une adresse en fonction de son nom définit dans le controlleur, on lui transmet en argument le nom de la route (@Route("", name="blog")) #}
                 <a class="p-2 link-secondary" href="{{ path('entrer') }}">Nouveaute</a>
                {#<a class="p-2 link-secondary" href="#">Culture</a>

                <a class="p-2 link-secondary" href="#">Business</a>
                <a class="p-2 link-secondary" href="#">Politics</a>  
                <a class="p-2 link-secondary" href="#">Travel</a> #}
                </nav>
            </div>
            

            <div class="p-4 p-md-5 mb-4 text-white rounded bandeau-header background-image: url("../img/jumbotron_bg.jpg");
background-size: cover;}" >
                <div class="col-md-6 px-0">
                <h1 class="display-4 fst-italic">RockNtro</h1>
                <p class="lead my-3">Ici vous trouverez le meilleur Top 50 des intros</p>
                <p class="lead mb-0"><a href="#" class="text-white fw-bold"></a></p>
            </div>
        </div>
    </div>

        <main class="container conteneur-base">
        {% block body %}
        
        {% endblock %}
        </main>
        <footer class="blog-footer">
            <p>&copy; 2021 - rockNtro - Tout est RockNRoll</p>
            <p>
                <a href="#">Revenir en haut</a>
            </p>
        </footer>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
    </body>
</html>

