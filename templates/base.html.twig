<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="description" content="RockNtro la reference des meilleures intros musicales selectionnées par la communautée des melomanes ">
        <title>{% block title %}rockNtro!{% endblock %}</title>
       
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
 
       <script> bootstrap.bundle.min.js/ bootstrap.bundle.js</script>

        {# CDN Bootstrap #}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('css/site.css') }}">
        <link rel="stylesheet" href="{{ asset('js/site.style.js') }}">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css"

    </head>

    <body>


        <div class="container">
            <header class="blog-header py-3">
                <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4 pt-1">

               {% if app.user %}
                            <span class="m-auto">Bonjour {{ app.user.pseudo}}</span>
                {% else %}
                            <a href="{{ path('app_register') }}" class=" btn btn-sm btn-outline-dark ml-2">Créer votre compte</a>
                {% endif %} 


                </div>
                    <div class="col-4 text-center">

                        <a class="blog-header-logo text-dark fst-italic" href="{{ path('home') }}"><img class="responsive" src="{{ asset('assets/logo_large.png') }}" alt=""></a>
                        <h1 class="display-9 lead my-3 slogan"><strong> Top 50 des intros musicales </strong></h1>

                </div>
                
                <div class="col-4 d-flex justify-content-end align-items-center">
                 
                     {% if app.user %}


                        <a href="{{ path('app_logout') }}" class="btn btn-sm btn-outline-dark">Se deconnecter</a>
                    {% else %}
                        

                        <a href="{{ path('app_login') }}" class="btn btn-sm btn-outline-dark ml-2">Se Connecter</a>

                    {% endif %}
                </div>

            </header>

            <div class="nav-scroller py-1 mb-2">
                <nav class="nav d-flex justify-content-evenly">

                <a class="p-1 link-dark" href="{{ path('home') }}"><i class="bi bi-house-door-fill"></i></a>


               <a class="p-0 link-dark" href="{{ path('entrer') }}">Nouveautés</a>



                {% if app.user %}


                <a class="p-2 link-dark" href="{{ path('proposition')}}">Proprosition</a>
                
                {% else %}
							{# <a href="{{ path('app_login') }}"> </a>  #}


                

							<a class= "link-dark p-2"'hoverPropos text-muted ' data-bs-toggle="tooltip" data-bs-placement="top" title="Se connecter pour faire une proposition" href="{{ path('app_login') }}">Proposition </a> 



				{% endif %}
  
                </nav>
            </div>
             

        <main class="container conteneur-base">
        {% block body %}
        
        {% endblock %}
        </main>

        <footer class="blog-footer">
            <i class="bi bi-instagram"></i>
            <i class="bi bi-facebook"></i>
            <i class="bi bi-twitter"></i>
            <p>&copy; 2021 - rockNtro - Tout est RockNRoll</p>
            <p>

                <a href="#">Revenir en haut</a>
               

            </p>
        </footer>

        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
        
    </body>
</html>

